# API

All endpoints are versioned under `/api/v1/`. This document describes what is
implemented; planned endpoints are marked as such and are not yet routable.

## Conventions

- **Correlation.** Every response carries `X-Request-ID`, generated by the
  server. A client-supplied value is discarded.
- **Errors.** RFC 7807 `application/problem+json`:

  ```json
  {
    "type": "about:blank",
    "title": "Not Found",
    "status": 404,
    "detail": "No such endpoint.",
    "instance": "/api/v1/nope",
    "request_id": "402bdbcf-274f-4e0a-ac14-13e75033bdd3"
  }
  ```

  `request_id` ties the failure to a backend log line. Error detail never
  contains internal diagnostics.
- **Planes.** Routes are grouped by authentication plane, each with its own
  middleware chain: health (open), agent (mTLS + Ed25519), client (OIDC +
  device attestation), SOC and admin (OIDC + RBAC).

## Implemented

### `GET /api/v1/health`

Liveness. Deliberately does not consult the database — a database outage must
not cause an orchestrator to kill healthy backends.

```json
{
  "status": "ok",
  "build": { "version": "0.1.0-dev", "commit": "unknown", "build_time": "unknown" },
  "checks": {},
  "time": "2026-08-31T08:23:09Z",
  "env": "development",
  "uptime": "15s"
}
```

### `GET /api/v1/health/ready`

Readiness, including dependency checks. Returns `200` when healthy and `503`
with `status: "degraded"` when a dependency is unavailable. The body is valid in
both cases, so the console can display the degradation rather than a blank
error.

```json
{
  "status": "ok",
  "build": { "version": "0.1.0-dev", "commit": "unknown", "build_time": "unknown" },
  "checks": { "database": "ok" },
  "time": "2026-08-31T08:23:09Z",
  "env": "development",
  "uptime": "15s"
}
```

Underlying errors are logged, never returned: driver errors can quote the
connection string.

## Planned

### Agent plane — mTLS plus Ed25519 signed body

| Method | Path | Phase |
|---|---|---|
| POST | `/api/v1/agent/enroll` | 3 |
| POST | `/api/v1/agent/heartbeat` | 3 |
| POST | `/api/v1/agent/posture` | 5 |
| POST | `/api/v1/agent/events` | 6 |
| GET | `/api/v1/agent/policy` | 9 |
| POST | `/api/v1/agent/session/attest` | 4 |

Headers: `X-NETRA-Device`, `X-NETRA-Nonce`, `X-NETRA-Signature`.

### Client plane — OIDC bearer plus device binding

| Method | Path | Phase |
|---|---|---|
| POST | `/api/v1/client/session/begin` | 4 |
| POST | `/api/v1/client/session/end` | 4 |
| GET | `/api/v1/client/me` | 2 |
| GET | `/api/v1/client/risk` | 7 |
| GET | `/api/v1/client/policy` | 9 |
| GET | `/api/v1/client/applications` | 11 |
| POST | `/api/v1/client/applications/{key}/launch` | 11 |
| GET | `/api/v1/client/stream` (SSE) | 7 |

### SOC plane — RBAC `SECURITY_ANALYST` / `AUDITOR`

| Method | Path | Phase |
|---|---|---|
| GET | `/api/v1/overview` | 10 |
| GET | `/api/v1/endpoints`, `/endpoints/{id}` | 3 / 10 |
| GET | `/api/v1/users`, `/users/{id}` | 2 / 10 |
| GET | `/api/v1/sessions`, `/sessions/{id}` | 4 / 10 |
| GET | `/api/v1/incidents`, `/incidents/{id}` | 12 |
| POST | `/api/v1/incidents/{id}/notes`, `/status` | 12 |
| GET | `/api/v1/risk/{session_id}` | 7 |
| GET | `/api/v1/events` | 6 |
| GET | `/api/v1/audit` | 2 |
| GET | `/api/v1/stream` (SSE) | 10 |

### Admin plane — RBAC `ADMIN`

| Method | Path | Phase |
|---|---|---|
| GET/POST | `/api/v1/policies` | 9 |
| POST | `/api/v1/policies/{id}/versions` | 9 |
| POST | `/api/v1/policies/evaluate` | 9 |
| POST | `/api/v1/devices/{id}/revoke` | 3 |
| POST | `/api/v1/enrollment-tokens` | 3 |
| GET/POST | `/api/v1/demo/scenarios` | 12 |

An OpenAPI 3.1 document is generated from Phase 2, once there are authenticated
routes worth describing formally.
